/dashboard:
  controller: Main
  action: index
  security: true

/login:
  controller: Security
  action: login

/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/page:
  controller: Main
  action: page

/admin:
  controller: Admin
  action: dashboard
  security: true
  role: [ADMIN,EDITOR]
  