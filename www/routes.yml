/:
  controller: Main
  action: index
  security: true

/login:
  controller: Security
  action: login

/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/page:
  controller: Main
  action: page

/addPage:
  controller: Main
  action: addPage

/modifyPage:
  controller: Main
  action: modifyPage

/deletePage:
  controller: Main
  action: deletePage

/admin:
  controller: Admin
  action: dashboard
  security: true
  role: [ ADMIN,EDITOR ]
  